# ============================================
# CHEMON 견적관리시스템 백엔드 환경 변수
# ============================================

# Database (필수)
# PostgreSQL 연결 문자열
DATABASE_URL="postgresql://user:password@localhost:5432/chemon_quotation?schema=public"

# JWT Authentication (필수)
# 32자 이상의 랜덤 문자열 권장
JWT_ACCESS_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-in-production"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS (프론트엔드 URL, 쉼표로 다중 origin 지원)
# 프로덕션: https://your-frontend-domain.vercel.app,https://your-preview.vercel.app
CORS_ORIGIN="http://localhost:3000"

# Rate Limiting
# 분당 최대 요청 수 제한
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
# debug, info, warn, error
LOG_LEVEL=info

# ============================================
# 프로덕션 배포 시 필수 변경 항목:
# 1. DATABASE_URL - Render PostgreSQL URL
# 2. JWT_ACCESS_SECRET - 32자 이상 랜덤 문자열
# 3. JWT_REFRESH_SECRET - 32자 이상 랜덤 문자열
# 4. NODE_ENV=production
# 5. CORS_ORIGIN - Vercel 프론트엔드 URL
# ============================================
